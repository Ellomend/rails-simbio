<%= form_with(model: nexu) do |form| %>
  <% if nexu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nexu.errors.count, "error") %> prohibited this nexu from being saved:</h2>

      <ul>
        <% nexu.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
